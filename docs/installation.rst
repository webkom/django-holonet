============
Installation
============

At the command line::

    $ easy_install django-holonet

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-holonet
    $ pip install django-holonet
